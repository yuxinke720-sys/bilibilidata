# 老师 VirtualBox Ubuntu 虚拟机 · 运行步骤（VS Code + .venv）

> 环境：Windows 主机上的 VirtualBox → Ubuntu 22.04 虚拟机（用户 `thnu`）。
> 代码实际在 **Ubuntu(Linux)** 里跑，IDE 用 **VS Code**，Python **3.8.10**，
> 虚拟环境 **`.venv`** 在项目目录内。项目放在 `~/桌面/Project/`（即 `/home/thnu/桌面/Project/`）。
> 本仓库代码全部是相对路径 + UTF-8，**在该 VM 上直接可跑，无需改路径**。

---

## 第 1 步：把代码放进 VM

把本项目拷进 `~/桌面/Project/`（用共享文件夹 / 拖拽 / git clone 都行）。
VS Code 里 **File → Open Folder** 打开这个 Project 目录。

## 第 2 步：装系统依赖（修复 "No module named pip"）

VS Code 里 **Terminal → New Terminal**，执行：
```bash
sudo apt update
sudo apt install -y python3.8-venv python3-pip
```

## 第 3 步：创建并激活虚拟环境 .venv

```bash
cd ~/桌面/Project           # 进项目目录
python3.8 -m venv .venv     # 若 .venv 已存在可跳过
source ./.venv/bin/activate # 激活
which python                # 应输出 /home/thnu/桌面/Project/.venv/bin/python
python3 --version           # 应显示 Python 3.8.10
```
> VS Code 右下角选解释器时，选这个 `.venv/bin/python`。

## 第 4 步：装项目依赖（一次装齐，替代逐个 pip install）

```bash
python -m pip install --upgrade pip
pip install -r requirements.txt
```
> 这一条等价于截图里的 `pip install faker requests brotli retry pandas`，
> 但 `requirements.txt` 还包含 Spark 分析/可视化要用的 `pyspark==3.2.0 jieba pyecharts numpy`，一次装全。
> 国内慢可加清华源：`pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`

## 第 5 步：（如需 HDFS）启动 Hadoop

老师镜像一般已配好 Hadoop。需要走 HDFS 时：
```bash
start-dfs.sh            # 启动 HDFS
jps                     # 应看到 NameNode / DataNode
```
不走 HDFS 只想快速验证，可跳过本步，分析脚本默认用本地 `file://` 读 txt。

## 第 6 步：按模块顺序运行

```bash
# 模块0 采集（已爬好 304 期在 ./data，无需重爬；要重爬才运行）
python get_bilibili_week.py

# 模块1 清洗 → 生成 bilibili_week.txt；--upload 顺带传 HDFS（需第5步已启动）
python data_process.py
python data_process.py --upload

# 模块2 Spark SQL：默认本地 file://；走 HDFS 加 --hdfs
python data_analysize1.py
python data_analysize1.py --hdfs

# 模块3 Spark MLlib
python data_analysize2.py
python data_analysize2.py --hdfs

# 模块4 可视化 → 生成 html/（入口 html/index.html）
python echarts_show.py
```

## 第 7 步：看结果

- `static/` 下是各分析 csv；`html/index.html` 是可视化汇总页（VS Code 里右键 → Open with Live Server，或文件管理器双击用浏览器打开）。

---

## 该 VM 上的注意点

- **从 `~/桌面/Project/` 目录运行脚本**，相对路径才对得上（`./data`、`./static`、`./html`）。
- 路径含中文"桌面"：分析脚本的本地 `file://` 已用 URI 编码处理，不会因中文路径报错。
- `hdfs dfs -put` 在 Linux 下可用；Windows 主机不参与运行，无需 winutils。
- 交付打包时，爬虫脚本按 PDF 规范改名为 `bilibili_week.py`（当前开发名 `get_bilibili_week.py`）。

> 本文件仅用于在 VM 上配置运行，不属于最终交付 zip 内容。
