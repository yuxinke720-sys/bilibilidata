# 课题报告书 草稿

> 用法：把下面各节内容贴进 `模板_课题报告书.docx` 对应位置。
> 🔴 标「【截图】」处需到机房老师镜像里跑代码截图后插入（可视化图可先用本地预览图占位）。
> 🟡 标「【你填】」处是只有你们组知道的信息。

---

## 一、项目基本信息

| 字段 | 内容 |
|------|------|
| 组别 | 第 **xx** 组 🟡【你填】 |
| 小组构成 | 🟡【你填：组员姓名，如 张三、李四、…】 |
| 项目名称 | B 站《每周必看》栏目数据分析 |
| 项目来源 | 厦门大学大数据平台 / Bilibili 网站项目 |
| 项目链接 | https://dblab.xmu.edu.cn/blog/2738/ |

**项目简介：**

Bilibili（简称 B 站）是中国年轻人高度聚集的文化社区和视频平台，人们可以通过弹幕、评论、点赞或收藏等方式来与视频发布者（称为 UP 主）进行互动。《每周必看》是 B 站的一个栏目，B 站会对新一周发布的视频根据不同条件进行筛选，将热门的、有趣的、有价值的视频收录在该栏目中推荐给用户观看。本次实验希望对被收录在 B 站《每周必看》的视频与 UP 主的数据信息进行研究，通过运用大数据处理框架 Spark、Hadoop 及数据可视化技术，对这些数据进行存储、处理和分析，并对每个收录视频能否进入热搜榜前 10 进行分类。

**实现功能：**

1. 采用爬虫技术，编写代码收集 B 站所有《每周必看》栏目的数据；
2. 对原始数据进行清洗，包括有价值字段的选择、异常数据的删除以及文本数据的修正等，最后数据保存为 txt 文件并上传至 HDFS；
3. 使用 Spark SQL 组件对 HDFS 的数据进行分析，主要统计各收录视频的基本播放情况以及 UP 主的累计数据；
4. 使用 Spark MLlib 组件对 HDFS 的数据进行分析，研究视频点赞数、播放量、互动热度等之间的关系，并训练机器学习模型对视频能否进入热搜榜前十进行分类；
5. 使用 pyecharts 工具对分析结果进行可视化。

---

## 二、项目实践环境

| 类别 | 配置 | 版本号 |
|------|------|--------|
| 操作系统 | Ubuntu | 22.04.2 LTS amd64 |
| 编程语言 | Python | 3.8.16 |
| IDE 选择 | PyCharm / Visual Studio Code / Anaconda | Anaconda 4.10.1 |
| 计算框架 | Spark | 3.2.0 |
| 存储框架 | Hadoop | 3.1.3 |

> 其他第三方库见 `requirements.txt`：jieba、pandas、pyecharts、requests、faker、brotli、retry、numpy 等。

---

## 三、项目实现流程（与"实现功能"一一对应）

### 1）数据集收集

爬虫代码在 `bilibili_week.py` 中，可在 PyCharm 中直接执行。B 站提供了 API 接口供开发者获取每一期《每周必看》的视频数据（`https://api.bilibili.com/x/web-interface/popular/series/one?number={}`，花括号内填入要收集的期数）。直接使用 requests 库发送请求并保存响应即可。为避免被网站识别为爬虫，借助 `fake_useragent` 在每次请求时随机使用一个 User-Agent，并通过 `retry` 让代码在当前期数爬取成功后才进行下一步。爬取后的数据直接转为 JSON 格式保存至当前目录的 `./data` 目录下，文件名形如 `week_序号.json`。本项目共采集了 304 期（第 20~326 期）数据。

🔴【截图】`bilibili_week.py` 关键代码 + `./data` 目录下的 json 文件列表

### 2）数据预处理

数据预处理阶段包括有效数据的选择、异常与空白数据的处理、文本数据的处理，代码在 `data_process.py` 中。

- **数据选择**：逐个读取 json 文件，数据包含在 `data` 字段中。其中 `config.name` 记录该期期数；`list` 是该期每个收录视频的具体情况（标题 title、描述 desc、推荐理由 rcmd_reason）；发布者信息在 `owner`（UP 主名 name）；视频的观看数、弹幕数、转发数等在 `stat` 中。从中选取期数、UP 主、标题、分区、view/danmaku/reply/favorite/coin/share/like/his_rank/dislike/desc/rcmd_reason 等字段保存到 DataFrame。
- **异常数据处理**：先删除含空值和重复的数据（**当 UP 主名字与视频标题同时相同视为重复**）；认为观看数 view、弹幕数 danmaku、评论数 reply、收藏数 favorite、投币数 coin、分享数 share、点赞数 like、历史排名 his_rank 小于等于 0，或不喜欢人数 dislike 大于 0 的数据为异常，一并删除。删除后 dislike 字段无意义，同样删列。
- **文本数据处理**：部分视频的 desc 和 rcmd_reason 为空，用视频标题对其填充；并清除换行符、Tab 等特殊符号，保证保存规范。
- **数据合并与上传**：对每个文件重复上述操作并不断合并 DataFrame，最后去除表头保存为文本文件 `bilibili_week.txt`（制表符分隔、UTF-8、LF），共 9953 条记录、14 列。再在终端执行 `hdfs dfs -put bilibili_week.txt /user/hadoop` 上传至 HDFS。

清洗后 txt 的列顺序：`period | up | title | tname | view | danmaku | reply | favorite | coin | share | like | his_rank | desc | rcmd_reason`。

🔴【截图】`data_process.py` 关键代码 + 生成的 `bilibili_week.txt` + `hdfs dfs -ls /user/hadoop` 上传成功

### 3）数据分析

进行数据分析前，先创建 SparkSession 与 SparkContext，用 `textFile` 读取 HDFS 上的文本数据存为 RDD；用 `map` 对每行按制表符切分并转为包含多个字段的 Row 对象；定义 schema 建立表头将 RDD 转为 DataFrame；最后注册为临时视图 `data`。

**① 基于 Spark SQL 的分析（`data_analysize1.py`，`spark-submit` 执行）**

- 统计收录视频最多的 UP 主：`count(up)` 按 up 分组降序，取前 10 → `popular_up.csv`；
- 统计收录次数最多的视频分区：`count(tname)` 按 tname 分组降序，取前 10 → `popular_subject.csv`；
- 播放量统计：选 `title, view` 降序取前 10 视频；`up, sum(view)` 分组降序取前 10 UP 主；弹幕、评论、收藏、投币、分享、点赞各指标同理，各出 Top10 视频与 Top10 UP 主 → `<指标>_video.csv` / `<指标>_up.csv`；
- 词频统计：用 jieba 对 title/desc/rcmd_reason 分词并去停用词（`chineseStopWords.txt`），统计词频，取前 300 且非空的词 → `<字段>_wordcount.csv`。

所有结果通过 `toPandas().to_csv()` 保存在 `./static/` 目录下。

🔴【截图】`data_analysize1.py` 关键代码 + `static/` 下生成的若干 csv

**② 基于 Spark MLlib 的分析（`data_analysize2.py`，`spark-submit` 执行）**

- 数据处理：选取 `['view','danmaku','reply','favorite','coin','share','like']` 研究其与历史排名 his_rank 的关系。新增 label 标签：`his_rank<=10 → 1`（曾进入热搜榜前 10），否则为 0。用 `VectorAssembler` 将特征组成特征向量，按 8:2 划分训练集与验证集；
- 变量相关性：用 `pyspark.ml.stat.Correlation` 计算特征间的斯皮尔曼相关系数，保存为 `correlation.csv`；
- 分类器训练：选用逻辑回归（最大迭代 15 次）、决策树、随机森林、GBT 四种分类器，根据上述特征和 label 训练，判断视频能否进入热搜榜前 10。用 `MulticlassClassificationEvaluator` 统计验证集分类正确率（Accuracy），用 `BinaryClassificationEvaluator` 得到二分类 AUC，结果保存为 `classifier_performance.csv`。

🔴【截图】`data_analysize2.py` 关键代码 + 训练集/验证集样本数 + 四分类器 Acc/AUC 结果

**③ 基于 pyecharts 的可视化（`echarts_show.py`，可在 PyCharm 中直接执行）**

读取 `./static/*.csv` 绘图，全部输出到 `./html/`：各互动指标的视频与 UP 主柱状图（Page 上下布局）；收录次数的分区/UP 柱状图 + 富文本饼图（Grid 左右布局）；title/desc/rcmd_reason 三个词云（Page 布局）；斯皮尔曼相关性热力图；四分类器 Accuracy/AUC 双柱状图。最后编写 `html/index.html`，通过超链接汇总各结果页的跳转。

🔴【截图】`html/index.html` 汇总页 + 各可视化图（投币、弹幕、收藏、点赞、评论、转发、播放量、分区占比、UP 占比、三词云、相关性热力图、分类器对比）

---

## 四、遇到的问题与解决方法

1. **关于 Spark 的版本**：项目在 conda 中新建了一个虚拟环境，在虚拟环境里 `pip install pyspark==3.2.0`，安装成功后实际使用的 Spark 版本与 pyspark 版本一致，而与虚拟机上单独安装的 Spark 无关。因此使用 Python + 虚拟环境完成作业时，可直接在虚拟环境里 `pip install pyspark==3.2.0` 解决 Spark 的安装，更加简便快捷。

2. **关于爬虫代码**：在 Windows 系统下爬虫总是出现"未能连接""拒绝请求"等问题，需反复执行才有几率成功；而在 Linux 环境下爬虫稳定不报错，因此最终数据的爬取在虚拟机（Linux）中进行。

3. **关于数据实时性**：B 站每个视频的数据（播放量、评论数、弹幕数等）会不断更新，因此每次运行代码得到的播放量、转发数等结果都会不同，报告及可视化中的结果是截止某一日各视频数据的快照。但就整体而言，短时间内的微小变化量不影响整体的分析结论。

---

## 五、总结

通过本次实验，我们较为完整地体验了大数据分析的全流程：数据爬取 → 数据清洗 → 上传 HDFS 存储 → Spark SQL 与 Spark MLlib 分析 → pyecharts 可视化。我们学会了如何编写爬虫采集数据、如何根据分析目标选择并清洗合适的数据、如何使用 Hadoop 和 Spark 工具对数据进行分布式存储与分析，以及如何把分析结果进行可视化呈现。

就本次使用的数据集与分析结果来看，B 站作为一个以娱乐为主的视频平台，搞笑、明星、游戏等相关视频更受用户喜爱；同时一些讲述日常或身边人物故事、引发对生活意义思考的生活类视频（如《二舅》等），容易与年轻人产生共鸣，也获得较高关注度。从机器学习的结果来看，仅凭播放量、评论数、弹幕数等互动数据可以对视频能否进入热搜前 10 进行初步分类，但准确率仍有提升空间，可能还需考虑 UP 主粉丝量、视频题材等更多信息。整个过程收获颇丰。

---

## 六、参考内容

1. B 站爬虫代码参考：和鲸社区，https://www.heywhale.com/mw/project/6059c0f0c910a9001581c98b
2. Python Spark 数据分析案例（厦门大学林子雨）：https://dblab.xmu.edu.cn/blog/2738/
3. pyecharts 可视化文档：https://pyecharts.org/#/zh-cn/intro
