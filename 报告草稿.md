# 课题报告书 草稿（第六组 · 最新版）

> 用法：把各节内容贴进 `模板_课题报告书.docx` 对应位置。
> 📷【截图N】= 需要你在**机房 VM 上跑真 Spark** 后截图插入的位置，已写清截哪里、该看到什么。
> 文字部分均为成稿，可直接用。数据口径：截至 2026 年最新一期（第375期）。

---

## 一、项目基本信息

| 字段 | 内容 |
|------|------|
| 组别 | 第六组 |
| 小组构成 | 🟡【填组员姓名，如 张三、李四、…】 |
| 项目名称 | B 站《每周必看》栏目数据分析 |
| 项目来源 | 厦门大学大数据平台 / Bilibili 网站项目 |
| 项目链接 | https://dblab.xmu.edu.cn/blog/2738/ |

**项目简介：**

Bilibili（简称 B 站）是中国年轻人高度聚集的文化社区和视频平台，用户可通过弹幕、评论、点赞或收藏等方式与视频发布者（UP 主）互动。《每周必看》是 B 站的一个栏目，B 站会对新一周发布的视频按不同条件筛选，将热门、有趣、有价值的视频收录其中推荐给用户。本次实验对被收录在《每周必看》的视频与 UP 主数据进行研究，运用 Spark、Hadoop 及数据可视化技术对数据进行存储、处理和分析，并对每个收录视频能否进入热搜榜前 10 进行分类。

**实现功能：**

1. 采用爬虫技术，编写代码收集 B 站《每周必看》栏目的数据；
2. 对原始数据进行清洗（字段选择、异常数据删除、文本修正等），保存为 txt 并上传至 HDFS；
3. 使用 Spark SQL 统计各收录视频的播放情况与 UP 主累计数据；
4. 使用 Spark MLlib 研究互动数据间的关系，并训练模型对视频能否进入热搜前十分类；
5. 使用 pyecharts 对分析结果进行可视化。

---

## 二、项目实践环境

| 类别 | 配置 | 版本号 |
|------|------|--------|
| 操作系统 | Ubuntu | 22.04.2 LTS amd64 |
| 编程语言 | Python | 3.8.16 |
| IDE 选择 | PyCharm / Visual Studio Code / Anaconda | Anaconda 4.10.1 |
| 计算框架 | Spark | 3.2.0 |
| 存储框架 | Hadoop | 3.1.3 |

> 其他第三方库见 `requirements.txt`：requests、faker、brotli、retry、jieba、pandas、pyecharts、numpy 等。

📷【截图1：环境版本】在 VM 终端依次执行 `python3 --version`、`hadoop version`、`pip show pyspark | grep Version`，截图各版本号，证明环境与上表一致。

---

## 三、项目实现流程（与"实现功能"一一对应）

### 1）数据集收集

爬虫代码在 `bilibili_week.py` 中。B 站提供 API 接口 `https://api.bilibili.com/x/web-interface/popular/series/one?number={}`（花括号填期数）。由于该接口已启用 **WBI 风控**（不带签名直接返回错误码 -352），爬虫的实现要点为：① 起一个 Session 访问主页获取 buvid3，再从 `nav` 接口取 WBI 密钥（img_key/sub_key）按固定表重排成 mixin_key；② 每次请求用 `w_rid + wts` 做 WBI 签名以通过风控；③ 借助 faker 随机 User-Agent、retry 自动重试，并每抓若干期更换 Session 以规避累计风控；④ 从 `list` 接口自动取当前最新期数，增量抓取（已存在的期跳过），只在返回 `code==0` 时保存。爬取的 JSON 保存至 `./data` 目录，文件名形如 `week_期号.json`。本项目共采集 **374 期**（第 1 期至第 375 期，其中第 105 期为 B 站官方空缺期号），时间跨度 **2019 年 3 月至 2026 年 5 月**。

📷【截图2：爬虫代码】截 `bilibili_week.py` 里 WBI 签名函数 `wbi_sign` + `get_weekdata_json` 的关键代码。
📷【截图3：爬虫运行结果】在 VM 跑 `python bilibili_week.py`，截末尾"新增 X 期 / 现共 374 个 json"的统计输出，以及 `ls ./data | wc -l` 的结果。

### 2）数据预处理

预处理代码在 `data_process.py` 中。

- **数据选择**：逐个读取 json，从 `config.name` 取期数，从 `list[].owner.name` 取 UP 主，从 `list[]` 取 title/desc/rcmd_reason/tname，从 `stat` 取 view/danmaku/reply/favorite/coin/share/like/his_rank/dislike，存入 DataFrame。
- **异常处理**：删除含空值与重复的数据（**UP 主名与视频标题同时相同视为重复**）；将 view/danmaku/reply/favorite/coin/share/like/his_rank≤0 或 dislike>0 的行视为异常删除；之后 dislike 字段无意义，删列。
- **文本处理**：desc、rcmd_reason 为空时用 title 填充，并清除换行符、Tab 等特殊符号。
- **合并与上传**：所有 DataFrame 纵向合并、去表头，保存为 `bilibili_week.txt`（制表符分隔、UTF-8、LF），共 **12487 行、14 列**；再用 `hdfs dfs -put` 上传至 HDFS 的 `/user/hadoop`。

清洗后 txt 列顺序：`period | up | title | tname | view | danmaku | reply | favorite | coin | share | like | his_rank | desc | rcmd_reason`。

📷【截图4：清洗代码】截 `data_process.py` 中去重/去异常/补文本的关键代码段。
📷【截图5：清洗运行结果】截 `python data_process.py` 输出的"读取 374 个 json → 去空/去重/去异常各删多少 → 保存 12487 行"的完整日志。
📷【截图6：HDFS 上传成功】截 `hdfs dfs -put -f bilibili_week.txt /user/hadoop/` 后 `hdfs dfs -ls /user/hadoop` 能看到该文件的结果（或 HDFS 网页 50070/9870 端口看到文件）。

### 3）数据分析

分析前先创建 SparkSession/SparkContext，用 `textFile` 读 HDFS 文本存为 RDD，用 `map` 按制表符切分转为 Row，定义 schema 转 DataFrame，注册临时视图 `data`。

**① Spark SQL（`data_analysize1.py`，`spark-submit` 执行）**

- 收录最多的 Top10 UP 主（`count(up)` 分组降序）→ `popular_up.csv`；
- 收录最多的 Top10 分区（`count(tname)` 分组降序）→ `popular_subject.csv`；
- 各互动指标（view/danmaku/reply/favorite/coin/share/like）的 Top10 视频与 Top10 UP（`sum`）→ `<指标>_video.csv` / `<指标>_up.csv`；
- 用 jieba 对 title/desc/rcmd_reason 分词去停用词，词频 Top300 → `<字段>_wordcount.csv`。
结果均 `toPandas().to_csv()` 存 `./static/`。

📷【截图7：SQL 代码】截 `data_analysize1.py` 中"RDD→Row→schema→注册视图"以及一两条 SQL（如 popular_up、词频）的代码。
📷【截图8：SQL 运行】截 `spark-submit data_analysize1.py --hdfs` 的运行过程 + `ls ./static` 生成的若干 csv。

**② Spark MLlib（`data_analysize2.py`，`spark-submit` 执行）**

- 选 `['view','danmaku','reply','favorite','coin','share','like']`，新增 label：`his_rank<=10→1` 否则 0；`VectorAssembler` 组特征向量，按 8:2 划分训练/验证集；
- 用 `Correlation` 计算斯皮尔曼相关 → `correlation.csv`；
- 训练逻辑回归（maxIter=15）、决策树、随机森林、GBT，用 `MulticlassClassificationEvaluator`(Accuracy) + `BinaryClassificationEvaluator`(AUC) 评估 → `classifier_performance.csv`。

📷【截图9：MLlib 代码】截 `data_analysize2.py` 中 label 生成、VectorAssembler、四分类器训练评估的代码。
📷【截图10：MLlib 运行结果】截 `spark-submit data_analysize2.py --hdfs` 输出的训练集/验证集样本数 + 四个分类器各自的 Accuracy 和 AUC。

**③ pyecharts 可视化（`echarts_show.py`，PyCharm 中直接执行）**

读 `./static/*.csv` 绘图输出到 `./html/`，并生成 `index.html` 汇总跳转页。

📷【截图11：可视化代码】截 `echarts_show.py` 中绘制柱状图/词云/热力图的关键代码。
📷【截图12：运行生成】截 `python echarts_show.py` 输出 + `ls ./html` 生成的全部 html。

---

## 四、可视化结果（逐图截图，打开 html/ 对应文件用浏览器截）

> 在 VM 用真 Spark 跑出 `html/` 后，逐个打开下列文件截图。每张图说明该看什么。

📷【截图13：汇总页】打开 `html/index.html`，截整页（含小电视背景 + 第六组作业水印 + 各结果跳转链接）。
📷【截图14：播放量】打开 `html/view.html`，截上下两个柱状图——播放量最多的 Top10 视频、累计播放量最多的 Top10 UP 主。
📷【截图15：投币数】打开 `html/coin.html`，截投币 Top10 视频与 UP 主。
📷【截图16：弹幕数】打开 `html/danmaku.html`。
📷【截图17：收藏数】打开 `html/favorite.html`。
📷【截图18：点赞数】打开 `html/like.html`。
📷【截图19：评论数】打开 `html/reply.html`。
📷【截图20：分享数】打开 `html/share.html`。
📷【截图21：分区收录占比】打开 `html/subject.html`，截左侧柱状图 + 右侧富文本饼图（搞笑、单机游戏、日常等分区收录次数）。
📷【截图22：UP 收录占比】打开 `html/up_count.html`，截收录次数最多的 UP 主柱状图 + 饼图。
📷【截图23：三词云】打开 `html/wordcloud.html`，截标题/简介/推荐理由三个词云。
📷【截图24：相关性热力图】打开 `html/correlation.html`，截七个互动指标的斯皮尔曼相关性热力图。
📷【截图25：分类器对比】打开 `html/classifier.html`，截四个分类器的 Accuracy/AUC 双柱状图。

**结果文字描述（可配图写）：** 从词云看，B 站语言以口语化、诙谐化为主，游戏领域（如原神）更受欢迎；分区上搞笑、单机游戏、日常收录最多。从热力图看，播放量越高的视频收藏、点赞往往越多，弹幕数与评论数相关性较大（都反映互动性）。从分类器看，仅凭播放量、评论数、弹幕数等可初步对视频能否进入热搜前 10 分类，但准确率仍有提升空间，可能还需考虑 UP 粉丝量、题材等信息。

---

## 五、遇到的问题与解决方法

1. **B 站接口风控（WBI 签名）**：采集时发现官方 `popular/series/one` 接口已升级风控，未带签名的请求直接返回错误码 `-352` 无法获取数据。通过分析得知需要 WBI 签名：先从 `nav` 接口取 img_key/sub_key 按固定表重排出 mixin_key，再对请求参数加 `wts` 时间戳并计算 `w_rid`（md5）签名，即可正常获取数据；同时通过随机 UA、每抓若干期更换 Session 进一步规避累计风控。

2. **关于 Spark 版本**：在 conda 新建虚拟环境后 `pip install pyspark==3.2.0`，实际使用的 Spark 与 pyspark 版本一致，与系统单独安装的 Spark 无关，更加简便。

3. **关于爬虫稳定性**：爬虫在 Windows 下常出现"未能连接""拒绝请求"等问题，需反复执行；在 Linux 环境下稳定，故数据采集在虚拟机中进行。

4. **关于数据实时性**：B 站视频的播放量、评论数等会不断更新，每次运行结果略有差异，报告中的结果为某截止日的快照；短时间内的微小波动不影响整体分析结论。

---

## 六、总结

本次实验较完整地体验了大数据分析全流程：数据爬取 → 清洗 → HDFS 存储 → Spark SQL 与 MLlib 分析 → pyecharts 可视化。我们学会了编写带风控签名的爬虫采集数据、按目标选择并清洗数据、使用 Hadoop 与 Spark 进行分布式存储与分析、以及将结果可视化呈现。就数据看，B 站作为娱乐为主的平台，搞笑、游戏、明星类视频更受欢迎；同时讲述日常或身边人物故事、引发对生活意义思考的视频（如《二舅》）也易与年轻人共鸣、获得高关注。整个过程收获颇丰。

---

## 七、参考内容

1. B 站爬虫参考：和鲸社区 https://www.heywhale.com/mw/project/6059c0f0c910a9001581c98b
2. Python Spark 数据分析案例（厦门大学林子雨）：https://dblab.xmu.edu.cn/blog/2738/
3. pyecharts 可视化文档：https://pyecharts.org/#/zh-cn/intro
4. B 站 WBI 签名机制参考（接口风控解决）：社区公开技术文档

---

> 截图总数 25 张，建议按编号命名（如 `截图14_播放量.png`）方便插入 Word。
> 重要：📷 标注的运行/结果截图，请在**机房 Ubuntu 虚拟机上用真 Spark 跑**后再截，确保 Acc/AUC 等数值是 Spark MLlib 的真实结果（与本地 pandas 预览略有差异）。
