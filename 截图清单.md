# 报告截图清单与图文教程（第六组）

> 用途：对照参考 PDF（见 [bili_pdf.txt](bili_pdf.txt)）逐张截图，做到「和老师一样详细」。
> 每张图给三样东西：**截哪里**（文件:行号 或 命令）、**📝 配什么讲解文字**（仿老师逐句解说，可直接贴 Word）、**该看到什么**。
> 环境：老师 Ubuntu 虚拟机，用户 `thnu@spark-class`，项目在 `~/桌面/bilibilidata-ubuntu-vm`。
> 代码截图在 PyCharm/VSCode 打开文件、滚到行号、框选即可，**无需跑程序**；运行图需按命令实际跑。

---

## 模块前：实验环境（截图1）

**截图1**｜命令截图：在 VM 终端依次执行
```bash
python3 --version
hadoop version
pip show pyspark | grep Version
```
📝 本项目在 Ubuntu 22.04.2 LTS 虚拟机中，用 PyCharm/VSCode + Python 实现。核心环境：Python 3.8、Hadoop 3.1.3、Spark 3.2.0（pyspark 自带），与实验标准配置一致。其余第三方库见 requirements.txt。
👀 看到三条命令各自打印的版本号。

---

## 模块0：数据集收集 `get_bilibili_week.py`（截图2 代码 + 截图3 运行）

**截图2-①**｜[get_bilibili_week.py:69-86](get_bilibili_week.py#L69-L86)（WBI 签名核心）
📝 官方 `popular/series/one` 接口已启用 WBI 风控，不带签名直接返回 `code=-352`。`get_mixin_key` 从 `nav` 接口取 `img_key/sub_key`，按 B 站固定重排表 `MIXIN_TAB` 拼成 32 位 `mixin_key`；`wbi_sign` 给请求参数加时间戳 `wts`，再对排序后的 query 拼接 mixin_key 做 md5 得到 `w_rid` 签名，即可通过风控。

**截图2-②**｜[get_bilibili_week.py:92-124](get_bilibili_week.py#L92-L124)（抓取单期）
📝 `get_weekdata_json` 用签名后的参数请求某一期，`@retry(tries=3)` 失败自动重试；对个别 br 压缩响应手动解压；仅当 `code==0` 且 `data` 非空时才用 `ensure_ascii=False` 存为 `week_期号.json`，避免存下风控或未发布的错误响应。配合随机 UA、每 12 期换一次 Session 进一步规避累计风控。
👀 看 `new_session`（随机 UA）与 `REFRESH_EVERY=12`（换 Session）也可一并入图。

**截图3**｜运行图：`python get_bilibili_week.py` + `ls ./data | wc -l`
📝 增量抓取：自动从 `list` 接口取最新期数，已存在的期跳过，新期优先。运行末尾打印「新增 X 期 / 跳过 / 失败」与 `./data 现共 N 个 json`。本项目共采集 374 期（第 1～375 期，105 期为官方空缺）。
👀 终端末尾的统计行 + `wc -l` 的数字（374 左右）。

---

## 模块1：数据预处理 `data_process.py`（截图4 代码 + 截图5 运行 + 截图6 HDFS）

**截图4-①**｜[data_process.py:50-79](data_process.py#L50-L79)（选字段）
📝 `parse_one_json` 打开单期 json，数据在 `data` 下分 config/list/owner/stat 几块：从 `config.name` 取期数，从 `list[].owner.name` 取 up，从 `list[]` 取 title/desc/rcmd_reason/tname，从 `stat` 取 view/danmaku/reply/favorite/coin/share/like/his_rank/dislike，存入记录。

**截图4-②**｜[data_process.py:108-126](data_process.py#L108-L126)（去空/去重/去异常）
📝 先 `dropna` 删核心字段含空值的行；再 `drop_duplicates(["up","title"])`，即 up 主名与标题同时相同视为重复；再把互动数或 his_rank≤0、dislike>0 的行判为异常删除；之后 `dislike` 字段无意义，`drop` 删列。

**截图4-③**｜[data_process.py:38-47](data_process.py#L38-L47) + [data_process.py:128-145](data_process.py#L128-L145)（补文本+保存）
📝 `clean_text` 把字段内换行符、Tab、回车替换为空格，保证制表符分隔不串列；desc/rcmd_reason 为空时用 title 填充；最后所有记录纵向合并、去表头，按 `\t` 分隔、UTF-8、LF 写出 `bilibili_week.txt`，列顺序与分析端 schema 严格一致。

**截图5**｜运行图：`python data_process.py`
📝 读取 374 个 json → 去空 0 条、去重 51 条、去异常 1504 条 → 用标题补空文本 → 保存 12487 行、14 列的 `bilibili_week.txt`。
👀 终端从「找到 374 个 json」到「已写出 …12487 行、14 列」的完整日志。

**截图6**｜HDFS 上传成功（**两块拼一张，仿 PDF**）

① 终端：
```bash
hdfs dfs -mkdir -p /user/hadoop
hdfs dfs -put -f bilibili_week.txt /user/hadoop/
hdfs dfs -ls /user/hadoop          # ⚠️ -ls 后面必须有空格！
```
（也可一条 `python data_process.py --upload`，脚本内部就是这几句，见 [data_process.py:162-168](data_process.py#L162-L168)）
👀 出现 `Found 1 items` 和 `-rw-r--r-- … 4901304 … /user/hadoop/bilibili_week.txt`。

② 网页（VM 内 Firefox）：`http://localhost:9870` → Utilities → Browse the file system → 路径填 `/user/hadoop` → Go!
👀 列表里有 `bilibili_week.txt`，Size ≈ 4.7 MB。
📝 将清洗后的数据上传至 HDFS，通过 `ls` 命令与 9870 网页浏览验证上传成功，至此数据清洗完成。
> 文件 4901304 字节 ≈ 4.7 MB（374 期，比 PDF 参考 2.6 MB 大，正常）。
> Mac 浏览器的 `localhost:9870` 指向 Mac 自己，打不开；要 Mac 看需 `hostname -I` 拿 VM 的 IP 并桥接/转发。建议直接在 VM 里截。

---

## 模块2/3：数据分析（截图7 SQL代码 / 截图8 SQL运行 / 截图9 MLlib代码 / 截图10 MLlib运行）

> PDF「3、数据分析」一节绝大多数是代码图，仅 2 张运行图（样本数、四分类器结果）。

### 分析前：RDD → DataFrame 准备
**图**｜[data_analysize1.py:101-143](data_analysize1.py#L101-L143)（`build_dataframe`）
📝 创建 SparkSession/SparkContext，用 `textFile` 读 HDFS 文本存为 RDD；`to_row` 用 `map` 按 `\t` 切分每行，前 4 列字符串、中间 8 个指标转整型、末尾两文本列字符串，封装成 `Row`，异常行返回 None 过滤；用 `StructType` 定义 14 列 schema 转 DataFrame，`createOrReplaceTempView("data")` 注册临时视图供 SQL 查询。

### 截图7：Spark SQL 代码 `data_analysize1.py`
**图 7-①**｜[data_analysize1.py:160-167](data_analysize1.py#L160-L167)
📝 up 名唯一，`count(up)` 为收录总次数；按 up 分组、count 降序取前 10，存 `popular_up.csv`。

**图 7-②**｜[data_analysize1.py:173-180](data_analysize1.py#L173-L180)
📝 `tname` 为视频分区，`count(tname)` 为该分区收录次数；分组降序取前 10，存 `popular_subject.csv`。

**图 7-③视频**｜[data_analysize1.py:186-194](data_analysize1.py#L186-L194)
📝 选 `title,view` 按 view 降序取前 10 得播放量最高的 10 个视频；danmaku/reply/favorite/coin/share/like 同理循环 `METRICS` 完成，分别存 `<指标>_video.csv`。

**图 7-③UP**｜[data_analysize1.py:196-201](data_analysize1.py#L196-L201)
📝 选 `up, sum(view)` 按 up 分组、总值降序取前 10 得累计最高的 10 个 up，存 `<指标>_up.csv`。

**图 7-④词频**｜[data_analysize1.py:207-226](data_analysize1.py#L207-L226)
📝 SQL 查 `title` 得 DataFrame，转 rdd 后 `flatMap(pretty_cut)` 分词展平；`map(word→(word,1))` 后 `reduceByKey` 聚合；`repartition(1)+sortBy` 降序；去空取前 300 转回 DataFrame 存 `title_wordcount.csv`，desc/rcmd_reason 同理。

**图 7-④pretty_cut**｜[data_analysize1.py:81-95](data_analysize1.py#L81-L95)
📝 `jieba.cut` 中文分词，去首尾空白、去停用词、过滤单字噪声后返回词语列表；停用词从相对路径 `chineseStopWords.txt` 加载。

### 截图8：Spark SQL 运行（PDF 无，自加，更充实）
**图 8**｜运行图：`spark-submit data_analysize1.py --hdfs` + `ls ./static`
📝 从 HDFS 读 12487 条注册视图 data，依次产出收录榜、各互动指标 Top10、词频 csv，全部存入 `./static/`。
👀 运行日志各「→ 已保存 …csv」+ `ls ./static` 列出的全部 csv。

### 截图9：Spark MLlib 代码 `data_analysize2.py`
**图 9-①label**｜[data_analysize2.py:116-122](data_analysize2.py#L116-L122)
📝 选 7 个特征列与 his_rank，新增 `label`：`his_rank<=10`（曾进热搜前十）为 1，否则 0。

**图 9-①向量+划分**｜[data_analysize2.py:124-128](data_analysize2.py#L124-L128)
📝 `VectorAssembler` 把 7 特征组装成 `features` 向量列；`randomSplit([0.8,0.2], seed=42)` 按 8:2 划分训练/验证集，固定 seed 可复现。

**图 9-②相关**｜[data_analysize2.py:136-143](data_analysize2.py#L136-L143)
📝 用 `pyspark.ml.stat.Correlation` 算特征间斯皮尔曼相关系数得 7×7 矩阵，转 pandas 加首列特征名存 `correlation.csv`。

**图 9-③分类器**｜[data_analysize2.py:149-177](data_analysize2.py#L149-L177)
📝 定义逻辑回归（maxIter=15）、决策树、随机森林、GBT 四种分类器；循环 `fit(train)` 训练、`transform(test)` 预测；用 `MulticlassClassificationEvaluator` 算 accuracy、`BinaryClassificationEvaluator` 算 AUC，两值越高性能越好，结果存 `classifier_performance.csv`。

### 截图10：Spark MLlib 运行（含 PDF 的两处运行图）
**图 10**｜运行图：`spark-submit data_analysize2.py --hdfs`
📝 输出训练集/验证集样本数（如 训练集 10065、验证集 2422），及四分类器各自 Accuracy/AUC。**一张图同时覆盖 PDF 的「样本数」与「四分类器结果」两处。**
👀 `[划分] 训练集 …条，验证集 …条` + 四行 `LogisticRegression … GBT` 的 Accuracy/AUC。

---

## 模块4：可视化 —— 代码截图（截图11，对齐 PDF「四、可视化」代码部分）

> 代码均在 [echarts_show.py](echarts_show.py)，读 `./static/*.csv` 输出到 `./html/`。可拆成下列小代码图，仿老师逐图讲解。

**图 11-①柱状图（互动指标）**｜[echarts_show.py:145-184](echarts_show.py#L145-L184)（`make_metric_pages`）
📝 分别从存视频指标与 up 累计指标的两个 csv 读数；为美观用 `add_line` 给长文本插换行符；`Bar()` 绘柱状图，`add_xaxis` 设横坐标、`add_yaxis` 设纵坐标、`set_global_opts` 设标题字体等全局项；最后 `Page(SimplePageLayout)` 把视频、UP 两图上下排版渲染成 `<指标>.html`。投币/弹幕/收藏/点赞/评论/转发流程同上。

**图 11-②收录次数（柱+富文本饼）**｜[echarts_show.py:190-230](echarts_show.py#L190-L230)（`make_count_page`）
📝 从 csv 读分区/up 的收录次数；`Bar()` 按收录次数绘柱状图；`Pie()` 绘富文本饼图，`formatter="{b}: {c} ({d}%)"` 显示名称、次数与占比；用 `Grid()` 把柱状图与饼图左右布局渲染成 `subject.html` / `up_count.html`。

**图 11-③词云**｜[echarts_show.py:236-256](echarts_show.py#L236-L256)（`make_wordcloud`）
📝 从词频 csv 读出 (词, 次数) 列表，用 `WordCloud` 绘词云（`word_size_range` 控字号、`shape="circle"`）；title/desc/rcmd_reason 三个词云用 `Page` 上中下布局渲染成 `wordcloud.html`。

**图 11-④热力图**｜[echarts_show.py:262-289](echarts_show.py#L262-L289)（`make_correlation`）
📝 从 `correlation.csv` 读相关矩阵，处理成 `[i,j,value]` 列表（i、j 为变量下标，value 为相关性）；用 `HeatMap` 绘热力图，`VisualMapOpts(min_=-1,max_=1)` 设色阶，渲染成 `correlation.html`。

**图 11-④分类器双柱**｜[echarts_show.py:295-313](echarts_show.py#L295-L313)（`make_classifier`）
📝 从 `classifier_performance.csv` 读各分类器性能，`Bar()` 两次 `add_yaxis` 分别添加 Accuracy 与 AUC，渲染成对比双柱状图 `classifier.html`。

**图 11-⑤汇总页**｜[echarts_show.py:319-371](echarts_show.py#L319-L371)（`make_index`）
📝 在 `/html` 下生成 `index.html`，用超链接列表汇总各结果页，点击文字即可跳转到对应可视化页面。

**截图12**｜运行图：`python echarts_show.py` + `ls ./html`
📝 main 依次执行上述绘图，所有 html 存入 `./html/`，入口为 `index.html`。
👀 运行日志各「→ 生成 …html」+ `ls ./html` 列出全部 html。

---

## 模块4：可视化 —— 结果截图（截图13-25，对齐 PDF「四、可视化结果」）

> 在 VM 用浏览器逐个打开 `html/*.html` 截图。📝 给的是解读模板——**结论请按你自己图里的实际榜首填写**（我们数据更新到 2026 年第 375 期，榜单可能与 PDF 2023 年的不同）。

**截图13 汇总页**｜`html/index.html`
📝 工程 `/html/index.html` 汇总各结果页，点击文字即可跳转。👀 整页含小电视背景 + 第六组作业水印 + 各跳转链接。

**截图14 播放量**｜`html/view.html`
📝 上图为播放量最高的 Top10 视频，下图为累计播放量最高的 Top10 UP 主。👀 写出榜首视频名与 UP 名（PDF 当年为《敢杀我的马？》、小潮院长，仅供参照）。

**截图15 投币数**｜`html/coin.html`　📝 投币最多的视频与累计投币最多的 UP。（PDF：《二舅》、老番茄）

**截图16 弹幕数**｜`html/danmaku.html`　📝 弹幕最多的视频与 UP。（PDF：《TF家族夏日运动会》、TF家族）

**截图17 收藏数**｜`html/favorite.html`　📝 收藏最多的视频与 UP。（PDF：《派大星的独白》、老番茄）

**截图18 点赞数**｜`html/like.html`　📝 点赞最多的视频与 UP。（PDF：《二舅》、老番茄）

**截图19 评论数**｜`html/reply.html`　📝 评论最多的视频与 UP。（PDF：《A-SOUL…隔岸》、原神）

**截图20 分享数**｜`html/share.html`　📝 分享最多的视频与累计分享最多的 UP。（PDF：《二舅》、原神）

**截图21 分区收录占比**｜`html/subject.html`
📝 左柱状图、右富文本饼图，展示各视频分区被《每周必看》收录的次数与占比。👀 搞笑、单机游戏、日常等分区领先。

**截图22 UP 收录占比**｜`html/up_count.html`
📝 收录次数最多的 UP 主柱状图 + 饼图。👀 写出收录次数最多的几位 UP。

**截图23 三词云**｜`html/wordcloud.html`
📝 标题/简介/推荐理由三个词云。结论：B 站语言以口语化、诙谐化为主，游戏领域（如原神）更受欢迎。

**截图24 相关性热力图**｜`html/correlation.html`
📝 七个互动指标的斯皮尔曼相关性热力图。结论：播放量越高的视频收藏、点赞往往越多；弹幕数与评论数相关性较大（均反映互动性）。

**截图25 分类器对比**｜`html/classifier.html`
📝 四种分类器 Accuracy/AUC 双柱状图。结论：仅凭播放量、评论、弹幕等可初步分类视频能否进热搜前十，但准确率仍有提升空间，可进一步考虑 UP 粉丝量、题材等信息。

---

> 本文件仅用于截图对照，不属于最终交付 zip。
> 正式报告正文可保持精简，Word 里要详细就从这份清单拷 📝 文字。
