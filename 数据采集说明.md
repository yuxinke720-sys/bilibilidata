# 数据采集说明：新旧爬虫对比与 WBI 升级

## 背景

B 站《每周必看》接口 `popular/series/one` 在 2025 年 6 月（约第 326 期）启用了 **WBI 风控**：不带签名的请求直接返回 `code=-352`。这导致项目早期的旧爬虫失效，必须升级为带 WBI 签名的新版本。这也是数据中 **week_1~326（旧版采集）** 与 **week_327~375（新版采集）** 分界的来历。

## 逐项对比

| 维度 | 旧版爬虫 | 现版本（get_bilibili_week.py） |
|---|---|---|
| WBI 签名 | ❌ 无，直接请求接口 | ✅ nav 取密钥 → 算 mixin_key → `w_rid`+`wts` 签名 |
| cookie | ❌ 写死硬编码（`bili_ticket` 等，会过期） | ✅ 动态：`new_session()` 访问主页自动获取 buvid3 |
| 期数范围 | ❌ `range(20, 327)` 写死，只到 326 | ✅ `get_latest_number()` 自动取最新期，抓全 |
| 增量/跳过 | ❌ 每次从头全抓 | ✅ 只抓缺的，已存在的 `week_*.json` 跳过 |
| 风控规避 | ❌ 无 | ✅ 每 12 期换新 Session + 重新签名，重置风控计数 |
| 有效性校验 | 仅 `try json.loads` | `code==0` 且 `data` 非空才保存，不存错误响应 |
| 存储目录 | `./dat`（拼写错误） | `./Data`（集中常量 `DATA_DIR`，可移植） |
| 编码 | 注释乱码（保存编码错误） | UTF-8，注释规范 |
| 代码结构 | 一坨，夹大量注释掉的死代码 | 函数化拆分、常量集中、可移植 |

## 两个关键改进

### 1. WBI 签名 —— 突破风控的核心

旧版靠「硬编码 cookie + 直接请求」，在接口启用 WBI 风控前尚可使用；`range(20, 327)` 写死了上限，最多抓到第 326 期。

升级版增加 WBI 签名流程：
1. 从 nav 接口取 `img_key` / `sub_key`；
2. 按固定重排表重排为 32 位 `mixin_key`；
3. 对请求参数加 `wts` 时间戳后按 key 排序，拼接 `mixin_key` 做 MD5 得到 `w_rid`。

成功绕过 `code=-352`，采集到第 327~375 期。

### 2. 动态获取 cookie —— 摆脱手贴失效

旧版的 `bili_ticket` 等是手动从浏览器复制的，`bili_ticket_expires` 到期后即彻底失效。升级版改为每次新建 `Session` 访问主页自动获取 buvid3 等基础 cookie，不再依赖人工维护。

## 小结

旧版（无 WBI、cookie 写死、期数写死到 326）是现版本的前身；现版本（WBI 动态签名、自动取最新期、增量抓取、换 Session 防风控）是为应对 B 站风控升级而重构的成果，也是本项目数据采集环节的主要技术亮点。
